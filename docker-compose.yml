services:
  stable-diffusion-webui:
    container_name: "framepack-studio"
    build:
     context: .
     dockerfile: Dockerfile
    command: --no-half --no-half-vae --precision full
    runtime: nvidia
    restart: unless-stopped
    ports:
      - 7860:7860
    volumes:
      - ${PWD}/.data/config.json:/app/config.json
      - ./.data/inputs:/app/inputs
      - ./.data/textual_inversion_templates:/app/textual_inversion_templates
      - ./.data/embeddings:/app/embeddings
      - ./.data/extensions:/app/extensions
      - ./.data/models:/app/models
      - ./.data/localizations:/app/localizations
      - ./.data/outputs:/app/outputs
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    deploy:
      mode: global
      placement:
        constraints:
          - "node.labels.iface != extern"
      restart_policy:
        condition: unless-stopped
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
